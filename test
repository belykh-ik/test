before_script:
  # Восстанавливаем файл из base64
  - echo "$HELM_VALUES_B64" | base64 -d > ./values.custom.${CI_PIPELINE_ID}.json
  - export HELM_CUSTOM_VALUES_FILE="./values.custom.${CI_PIPELINE_ID}.json"
  - cat "$HELM_CUSTOM_VALUES_FILE"