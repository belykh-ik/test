import { defineConfig } from 'vite'

export default defineConfig({
  preview: {
    host: true,               // или '0.0.0.0'
    port: 3000,
    allowedHosts: [
      'test-inspection.lee.sogaz.ru',
    ],
  },
})
