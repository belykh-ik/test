docker exec -it rabbitmq rabbitmqadmin publish exchange=amq.default routing_key=events \
  payload='{"stream":"a","message":"hello a"}' \
  --username=dev --password=dev
----
curl --cacert tls/ca.crt -u admin:admin "https://localhost:9200/_cat/indices?v"
-----
