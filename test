docker-build:
  stage: build
  rules:
    - if: $CI_COMMIT_TAG
      when: always
    - when: never
  script:
    - docker build -t registry/app:$CI_COMMIT_TAG .
    - docker push registry/app:$CI_COMMIT_TAG