JAR_PATH=$(find . -type f -name 'edo-data-mart-api-*.jar' -print -quit)
JAR_FILE=$(basename "$JAR_PATH")
docker build --build-arg JAR_PATH="$JAR_PATH" --build-arg JAR_FILE="$JAR_FILE" -t "$CURRENT_IMAGE" .