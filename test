pluginManagement {
    repositories {
        maven { url = uri("https://nexus.company.local/repository/maven-public/") }
        gradlePluginPortal() // можно оставить, если есть интернет, либо удалить
        mavenCentral()
    }
}