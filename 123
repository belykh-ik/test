# Удаляем пустые строки и кавычки, убираем пробелы вокруг "="
sed -i '/^$/d' .env
sed -i 's/["'\'']//g' .env
sed -i 's/ *= */=/' .env

# Проверяем, что все строки валидны
grep -E '^[A-Za-z_][A-Za-z0-9_]*=.*$' .env || { echo "❌ Invalid .env format"; exit 1; }